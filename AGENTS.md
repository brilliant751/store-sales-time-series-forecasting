# 仓库协作规范

项目结构、说明、运行 参阅：[README.md](README.md)。


## General

1. 未经确认，不修改已有实验性或关键文件（如 `data/` 中的数据、`output/` 中的模型）。
2. 询问确认：对于以下操作必须在继续前询问人类用户确认：
   - 运行耗时较长的训练（>10 分钟）或消耗大量资源的命令（尤其是 GPU 作业）。
   - 下载或上传大量/敏感数据到远程服务。   
3. 所有代码相关的改动后，必须至少运行相关测试。

## Commit

- 修改时：保持提交原子性，一个 PR/提交只做一类改动（如“修复数据加载”或“新增特征工程”）。
- 提交描述：简短明了，包含“目的”和“影响范围”。

## More

- 若项目中存在未说明的脚本或约定（例如自定义训练入口、特殊配置路径），请先在仓库中搜索 `src/` 与 `configs/`，并将发现以列表形式发给用户确认。


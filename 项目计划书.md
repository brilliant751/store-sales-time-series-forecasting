# 《数据分析与挖掘》期末项目计划书

## 一、项目基本信息
- 课程项目主题：跨领域数据挖掘竞赛——从数据到决策的全流程实战
- 项目选项：选项 A（多源数据融合挖掘）
- 项目题目：基于多源时序数据的门店-商品销量预测
- 参考竞赛：[Kaggle Store Sales - Time Series Forecasting](https://www.kaggle.com/competitions/store-sales-time-series-forecasting/overview)
- 团队规模：4 人（固定）
- 项目周期：5 周

## 二、选题背景与目标
零售业务中的销量预测是供应链与库存优化的核心问题。门店在不同日期、不同商品维度上的销量受促销、节假日、门店属性、外部经济环境等多因素影响，具有明显的时序性、非平稳性和跨序列异质性。

本项目拟构建一个可复现、可解释、可对比的销量预测系统，核心目标如下：
1. 预测不同门店产品在未来日期的销售量。
2. 充分融合多源信息（节假日、油价、门店属性、促销等），提升预测精度。
3. 在选项 A 框架下对多种模型进行对比，分析各方法优劣并选择最优方案。
4. 交付可一键运行的完整代码、实验报告与答辩展示材料。

## 三、数据来源与数据理解
项目使用 Kaggle 提供的多表结构化时序数据集：
1. `train.csv`：历史销售数据（日期、门店、产品、促销、销售额等）。
2. `test.csv`：未来待预测日期。
3. `stores.csv`：门店属性（城市、类型、聚类等）。
4. `oil.csv`：每日油价（外部经济 proxy 变量）。
5. `holidays_events.csv`：节假日与事件信息。

### 3.1 数据特点与挑战
1. 多源异构：主事实表 + 多维辅助表，需要进行稳定的数据关联与特征对齐。
2. 时序复杂：存在趋势、季节性、节假日冲击与促销脉冲效应。
3. 大规模多序列：不同门店-商品组合分布差异大，可能存在冷启动与长尾问题。
4. 缺失与噪声：油价、节假日可能有缺失、异常日期定义或冗余字段。

### 3.2 预处理与融合思路
1. 时间字段标准化（`date` 解析、周/月/季度等派生）。
2. 多表 join 与主键校验（`store_nbr`、`family`、`date`）。
3. 缺失值处理（前向填充/插值/分组统计填充，对比验证）。
4. 异常值识别（销量尖峰/异常波动标注）。
5. 特征工程：
   - 时间特征：星期、月份、是否月初月末、节前节后窗口。
   - 滞后特征：`lag(1,7,14,28)`、滚动均值/方差。
   - 事件特征：节假日类型、是否本地节日、事件强度编码。
   - 外生特征：油价及其变化率、促销强度。
   - 门店特征：城市、类型、聚类类别编码。

## 四、方法设计与算法对比
在选项 A（多源数据融合）的任务框架下，计划实现并对比以下 5 类方法，以验证多源特征与建模策略的有效性：
1. **季节性朴素基线（Seasonal Naive）**：作为最低可比基线。
2. **ARIMA/SARIMA**：传统统计时序模型，验证线性假设下性能。
3. **Prophet**：对趋势+季节+节假日建模，便于解释。
4. **XGBoost（特征工程版）**：使用滞后与外生特征的监督学习方案。
5. **LSTM/GRU**：深度时序模型，捕捉复杂非线性依赖。

> 备选增强：Temporal Fusion Transformer（如时间允许，作为扩展实验）。

### 4.1 实验设计
1. 时间切分验证（避免数据泄漏）：按日期滚动验证（rolling-origin）。
2. 统一评估指标：
   - 主指标：`RMSLE`（与竞赛一致）
   - 辅指标：`MAE`、`RMSE`
3. 消融实验：
   - 去除节假日特征
   - 去除油价特征
   - 去除促销特征
   对比多源信息带来的增益。
4. 统计显著性分析：关键模型差异做配对检验（如 Diebold-Mariano 或 bootstrap 置信区间）。

## 五、团队成员与分工
> 原则：主责明确、跨角色协作；每位成员需在仓库与报告中留下可量化贡献证据（commit、文档作者署名、实验记录）。

1. **陈攀（2353243）— 数据工程负责人**
   - 负责数据清洗、多表融合、数据质量检查、特征流水线搭建。
   - 输出：`data_pipeline/` 代码、特征文档、数据血缘说明。

2. **薛漕洋（2350240）— 算法负责人（统计模型方向）**
   - 负责 ARIMA/SARIMA、Prophet 建模与参数调优。
   - 输出：统计模型训练脚本、实验记录、阶段性结论。

3. **滕其峰（2351359）— 算法负责人（机器学习/深度学习方向）**
   - 负责 XGBoost、LSTM/GRU 建模、特征选择与训练优化。
   - 输出：模型代码、调参日志、模型对比分析图表。

4. **朱从周（2351893）— 评估分析与工程交付负责人**
   - 负责评估体系、可视化分析、结果汇总、README、答辩材料整合。
   - 输出：评估脚本、可视化图表、项目报告整合稿、PPT 初稿。

## 六、进度安排（5 周）
1. **第 1 周：需求细化与数据摸底**
   - 明确任务边界、评估指标、目录规范；完成 EDA 与数据质量审查。
2. **第 2 周：数据工程与基线模型**
   - 完成可复用数据流水线；实现 Seasonal Naive + 初版 ARIMA。
3. **第 3 周：核心模型开发**
   - 完成 Prophet、XGBoost、LSTM/GRU 训练与初步调参。
4. **第 4 周：系统对比与消融实验**
   - 完成统一验证、指标汇总、特征贡献与误差分析。
5. **第 5 周：交付打磨与答辩准备**
   - 完整代码封版、一键运行主流程、报告定稿、PPT 与演示排练。

## 七、预期交付物
1. 项目计划书（本文件）。
2. 代码仓库（GitHub/Gitee，完整 commit 历史，成员贡献可追踪）。
3. 完整代码（模块化、带注释、可一键运行主流程）。
4. 项目报告（10-20 页：摘要、引言、方法、实验、讨论、结论）。
5. 团队分工声明（到文件级/章节级，附工作量比例）。
6. 答辩 PPT + 演示视频（可选，10 分钟讲解+系统演示/结果展示）。
7. 项目海报（加分项，A0/A1 尺寸）。

## 八、风险与应对
1. **数据泄漏风险**：严格时间切分，训练集与验证集按时间先后划分。
2. **模型训练耗时风险**：采用分层抽样调参、设置早停、优先基线稳定性。
3. **多人协作冲突风险**：采用 Git 分支策略（feature/*），每周固定合并窗口。
4. **结果不稳定风险**：设置随机种子、多次复现实验，报告均值与方差。
5. **进度延误风险**：关键里程碑提前 2-3 天完成，预留答辩缓冲期。

## 九、仓库规范（执行约定）
1. 目录建议：
   - `data/`（仅放说明或样例，不上传大体积原始数据）
   - `src/data_pipeline/`
   - `src/models/`
   - `src/evaluation/`
   - `notebooks/`
   - `reports/`
   - `slides/`
2. 每次提交使用清晰 commit message（示例：`feat(model): add prophet baseline`）。
3. 关键实验必须记录：参数、数据切分、指标、运行时间、结论。

## 十、项目成功标准
1. 至少完成 5 种方法的统一对比，并给出可解释结论。
2. 多源特征（节假日、油价、门店属性等）对性能提升有量化证据。
3. 代码可复现，主流程可运行，文档齐全。
4. 团队成员贡献可核验、分工清晰、交付按时完成。
